# Inheritance

- res in the handler of http.createServer()
  - http.ServerResponse
    - http.OutgoingMessage
      - stream.Stream

- `stream.Transform`
  - `stream.Duplex`
  
  - It implements
    - `writable._write()`
    - `readable._read()`